"use client";

import Link from "next/link";

const Logo = () => {
  return (
    <Link href="/" className="relative">
      <span className="font-orbitron text-xl font-bold text-cyan-500">
        CYBER_DEV
      </span>
    </Link>
  );
};

export default Logo;
